apiVersion: v1
kind: Service
metadata:
  name: authentication-server-example-service
spec:
  selector:
    app: authentication-server-example
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
